# Pinned to compatible-release ranges: patch and minor updates are allowed,
# a major bump is not. The unpinned `>=0.2` this replaced would today install
# langgraph 1.x against code written for 0.2.x, so a fresh clone was one
# upstream release away from breaking for reasons the reader could not see.
langgraph~=1.2.11
langchain-openai~=1.6.0
langchain-core~=1.6.1
pydantic~=2.13.5
python-dotenv~=1.2.3
requests~=2.34.2
tenacity~=9.1.4
opentelemetry-api~=1.44.0
opentelemetry-sdk~=1.44.0
