"""Incident-triage agent: logs -> git history -> issues -> root-cause report.

An orchestrated (LangGraph) agent where each investigation phase is an
independently controllable, swappable node.
"""
__version__ = "0.1.0"
